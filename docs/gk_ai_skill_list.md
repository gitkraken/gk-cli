## gk ai skill list

List AI coding tools and whether GitKraken skills are installed

### Synopsis

List the AI coding tools GitKraken skills can be installed for, which of
them are detected on this machine, and which already have the skills installed.

Supported clients: claude-code, opencode, cursor, codex, copilot, antigravity

```
gk ai skill list [flags]
```

### Examples

```
  gk ai skill list
  gk ai skill list --output json
  gk ai skill list --output json --fields name,installed
```

### Options

```
      --fields string   Comma-separated list of fields to keep in JSON output (e.g. name,installed); requires --output json
  -h, --help            help for list
```

### Options inherited from parent commands

```
      --no-telemetry     Disable telemetry (OTel spans, Sentry) for this invocation
      --output string    Output format: text or json (default "text")
      --session string   (Optional) Isolate auth and cache under a named session
```

### SEE ALSO

* [gk ai skill](gk_ai_skill.md)	 - Manage skills for AI coding tools

###### Auto generated by spf13/cobra on 31-Aug-2026
