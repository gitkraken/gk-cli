## gk ai provider set

Update fields on a registered AI provider

```
gk ai provider set <provider> [flags]
```

### Examples

```

  gk ai provider set openai --model gpt-4o-mini
  gk ai provider set anthropic --key sk-ant-new...
  gk ai provider set gitkraken --model anthropic:claude-sonnet-4-5
  gk ai provider set gitkraken --model ""   # clear the override
```

### Options

```
  -h, --help           help for set
      --key string     Replace the API key
      --model string   Replace the model id
      --url string     Replace the base URL override
```

### Options inherited from parent commands

```
      --no-telemetry     Disable telemetry (OTel spans, Sentry) for this invocation
      --output string    Output format: text or json (default "text")
      --session string   (Optional) Isolate auth and cache under a named session
```

### SEE ALSO

* [gk ai provider](gk_ai_provider.md)	 - Manage AI providers

###### Auto generated by spf13/cobra on 17-Aug-2026
