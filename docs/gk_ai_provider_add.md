## gk ai provider add

Register a new AI provider

```
gk ai provider add <provider> [flags]
```

### Examples

```

  gk ai provider add openai --key sk-... --model gpt-4o
  gk ai provider add anthropic --key sk-ant-... --model claude-sonnet-4-5
  gk ai provider add openrouter --key sk-or-... --model anthropic/claude-sonnet-4
  gk ai provider add ollama --model llama3 [--url http://localhost:11434]
```

### Options

```
  -h, --help           help for add
      --key string     API key for the provider (not required for ollama)
      --model string   Model id to use (e.g., gpt-4o, claude-sonnet-4-5, llama3)
      --url string     Override the provider's base URL (optional; defaults are used otherwise)
```

### Options inherited from parent commands

```
      --no-telemetry     Disable telemetry (OTel spans, Sentry) for this invocation
      --output string    Output format: text or json (default "text")
      --session string   (Optional) Isolate auth and cache under a named session
```

### SEE ALSO

* [gk ai provider](gk_ai_provider.md)	 - Manage AI providers

###### Auto generated by spf13/cobra on 17-Aug-2026
